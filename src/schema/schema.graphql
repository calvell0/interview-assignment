type Query {
  address(username: String!): Address
  nearEarthObjects(startDate: String!, endDate: String!): NearEarthObjectFeed
}

type Mutation {
  createAddress(username: String!, input: AddressInput): Address
}

input AddressInput{
  street: String!
  city: String!
  zipcode: String!
  state: String!
}
