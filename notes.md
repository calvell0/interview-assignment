# Ticket 3: 
- I'm using a non-envelop plugin to handle this because to my understanding, it's not possible to reject the request itself from an envelop plugin. If I were to use an envelop plugin, then I would just be throwing a GQL error, and invalid headers are more of an authorization issue than a GQL issue IMO.
- had to do some googling around to figure out how to read the operation name since the operationName field isn't populated 

# Ticket 4: 
- needed to make 2 changes to fix request id: 1, add winston formatter that reads from the logger's requestId to the logger constructor. 2, change buildHeaders to build headers during parse stage so that the requestId is available in the context in the execution stage.


# Ticket 5: 
- had to change the test executor to pass the client header


## Sprint 2 (Bonus)

- I'm a bit stuck - I think I'm like 75% there but I can't seem to get the query to execute without issues. I'm consistently running into this error: 
 [GraphQLError: Failed to create a gateway request. The request must contain at least one operation.],
- even though the info object I'm passing in nearEarthObject.ts has an operation object within the info object. I also was at other points getting an error where it was claiming there was no selectionSet passed, even though there was. I suspect that these errors are stemming from the fact that I haven't figured out how to resolve the date-keyed object into a flattened array in the mesh layer instead of in the yoga layer - since no selectionSet I give it will match fields that are actually dates. If I were to continue working on this, I would keep trying to figure out how to configure the json-schema handler to somehow tell mesh to flatten that object for me. Another approach I took was specifying additionalResolvers in the .meshrc.yml file, but that also didn't work because of the field naming mismatch between the autogenerated mesh gql schema and the yoga-side schema I made myself (you can see this attempt in /mesh-resolvers, it was inspired by these docs: https://developer.adobe.com/graphql-mesh-gateway/mesh/advanced/extend/resolvers/programmatic-resolvers). I tried my hardest to read around and find examples of this use case but unfortunately I couldn't find anything before I started to run out of time. 